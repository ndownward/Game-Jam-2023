using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Player : MonoBehaviour
{
    public GameObject rootSelected;

    // Update is called once per frame
    void Update()
    {
        CheckRopeInputs();
    }

    void CheckRopeInputs()
    {
        if(Input.GetMouseButtonDown(0))
        {
            
        }
    }
}
